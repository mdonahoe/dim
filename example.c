/* Example C file for syntax highlighting tests */
#include <stdio.h>

int main(void) {
    int x = 42;
    char *message = "Hello from C!";

    if (x > 0) {
        printf("%s\n", message);
        return 0;
    }
    return 1;
}
